server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/client-processing_db
    username: postgres
    password: Dima_2145
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration

  jpa:
    hibernate:
      ddl-auto: validate
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    properties:
      format_sql: true